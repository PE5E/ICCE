#include "main.ih"
#include <iostream>
#include <cstring>      // memcpy
using std::memcpy;

int main(int argc, char **argv)
{
//    Matrix mat{3, 5};
//    mat(2,2, Mode::BY_COL);
//    cin >> mat(2,2, Mode::BY_COL);
//    show(std::cout, mat);
//    cin >> mat(2,3, Mode::BY_ROW);
//    show(std::cout, mat);
//
//    Matrix mati{4, 8};
//    cin >> mati(Mode::BY_COL);
//    show(std::cout, mati);

    Matrix matr{4,8};

}



