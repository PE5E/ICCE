//This constructor is commented out in the header. see header for more info.

template <template <typename> class Container, typename Data>
Insertable<Container, Data>::Insertable(Data &&data)
:
    Container<Data>(1, data)
{}
